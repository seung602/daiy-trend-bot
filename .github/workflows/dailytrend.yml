- name: Check Gemini configuration
  run: |
    python - <<'PY'
    import os

    key = os.getenv("GEMINI_API_KEY", "")
    model = os.getenv("GEMINI_MODEL", "")

    print("GEMINI_API_KEY exists:", bool(key))
    print("GEMINI_API_KEY length:", len(key))
    print("GEMINI_MODEL:", model)
    PY
